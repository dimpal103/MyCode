package string;

public class DelString {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		//Q : What will be length of string “123  “ ?
        
		//Q : What will be value of toUpperCase of “dimpal.tarwan1” ?

		//Q : Write a function which extracts last 3 characters in the given string.
		//e.g. if input = “Anamika”, then output should be “ika”;
		//If input = “1234”, then output should be “234”.

		//Q : Write a function, which takes file_name as input and return file extension as output.
		// e.g. input  = “abc.txt”, output = “txt”;
		//input “C://ab/myfile.jpg”, output = “jpg”.

		//In lexicographical order Capital letters come before small letters.
		//So if we sort “A”, “b”, “B”, “c”, we will get “A”, “B”, “b”, “c”.

		//Q : What will be value of comparison of “100” and “21” ?


		//Q : Define this function.
		// Returns true if firstString contains secondString ignoring case.
		// Returns false otherwise.

		//Splits the string based on regex. e.g.
		//String s = “abc, 4, 5”;
		//String[] parts = s.split(“,”);
		//In string array , parts[0] will be “abc”, parts[1] = 4 and parts[2] will be 5.

		//Q : Split “Anamika Kumari” using space.
		//Q : Split “Mukesh 40 67 50” using space.

		//String trim()
		//Removes the whitespace(space, tab, newline) from beginning and end of string.
		//e.g.
		//String s = “   Anamika Kumari  “;
		//String s1= s.trim();
		//Value of s2 will be “Anamika Kumari”;

		//String replaceAll(String regex, String replacement);
		//Replaces String  which match ‘regex’ with replacement. e.g.

		//String s = “1==2==3-4”;
		//String s2 = s.replaceAll(“==”, “,”);
		//Value of s2 will be 1,2,3-4


	}

}
